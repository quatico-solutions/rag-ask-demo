# LM Studio Configuration
# Make sure LM Studio is running and has models loaded

# Provider settings
AI_COMPLETION_PROVIDER=lmstudio
AI_EMBEDDING_PROVIDER=openai  # LM Studio typically doesn't support embeddings well

# LM Studio API endpoint
LMSTUDIO_BASE_URL=http://localhost:1234/v1

# Model loaded in LM Studio (check the exact name in LM Studio)
# This MUST match the model name shown in LM Studio's UI
AI_COMPLETION_MODEL=TheBloke/Mistral-7B-Instruct-v0.2-GGUF

# For embeddings, use OpenAI since LM Studio doesn't handle them well
AI_EMBEDDING_MODEL=text-embedding-ada-002
OPENAI_API_KEY=your-openai-api-key-here